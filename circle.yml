dependencies:
  pre:
    - cp config/database.yml.ci config/database.yml

database:
  override:
    - bundle exec rake db:create db:schema:load --trace

machine:
  ruby:
    version: 2.3.1

test:
  pre:
    - bundle exec rubocop
  post:
    - bundle exec rake circleci:coverage
