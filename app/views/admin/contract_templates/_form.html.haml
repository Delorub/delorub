%h2 Редактор шаблонов договоров

= semantic_form_for [:admin, @contract_template] do |f|
  = f.inputs do
    = f.input :title
    = f.input :category
  = react_component 'ContractDesignerApp'
  = f.actions

// = javascript_include_tag 'contract_designer_app-bundle'
