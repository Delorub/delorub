.dr-block-style.dr-block-style--category
  .subcategories
    p.subcategories__header
      | Город
    .subcategories-list
      = content_tag 'custom-multiselect', '',
        'v-model': 'city_id',
        ':searchable': 'false',
        ':allow-empty': 'false',
        'placeholder': 'Выберите город',
        ':options': City.active.pluck(:name).to_json,
        '@select': 'populateSubcategories'
.dr-block-style.dr-block-style--category
  .subcategories
    p.subcategories__header
      | Категории
    .subcategories-list
      ul.subcategories-list__sup
        - all_categories.each do |category|
          li
            = link_to category.title, category_tasks_path(category), class: @category && @category.id == category.id ? 'active' : ''
            - if @category && (@category.id == category.id || @category.parent.present?) && category.children.present?
              ul.subcategories-list__sub
                - category.children.each do |sub_category|
                  li
                    = link_to sub_category.title, category_tasks_path(sub_category), class: @category && @category.id == sub_category.id ? 'active' : ''
