<div class="col-sm-1 col-sm-pull-11 dr-side-menu">
  <br>
  <br>
  <a href="#" class="gamburg-menu icon-gamb_menu"></a>
  <div class="totalz">
    <a href="<%= my_tasks_path %>"><span class="n1"><%= current_user.tasks.count %></span><span class="txa">Мои<br> задания</span></a>
  </div>
  <nav>
    <ul class="left_menu">
      <% if policy(Task).create? %>
        <li><a href="<%= new_task_path %>"><span class="icon_top icon-add"></span>Создать<br> задание</a></li>
      <% end %>
      <% if policy(Profile).create? %>
        <li><a href="<%= new_profile_path %>"><span class="icon_top icon-zadacha"></span>Создать<br> анкету</a></li>
      <% end %>
      <% if user_signed_in? && current_user.master? && policy(current_user.profile).edit? %>
        <li><a href="<%= edit_profile_path(current_user.profile) %>"><span class="icon_top icon-setups"></span>Моя<br> анкета</a></li>
      <% end %>
    </ul>
  </nav>
  <div class="year">© 2017</div>
</div>
